#v(1.1em)

== 支撑材料文件列表

逐项列出最终 ZIP/RAR 中的每个文件名和用途，必须与压缩包完全一致。不要列入赛题直接提供的原始数据。

#table(
  columns: (1fr, 2fr),
  [*文件名*], [*用途*],
  [PLACEHOLDER：code/main.py], [PLACEHOLDER：生成论文全部结果与图表],
)

== 完整源程序

必须在本附录嵌入建模使用的全部完整、可运行源程序及 Excel/SPSS 等交互命令；同时把相同文件放入支撑材料。按实际文件逐一加入，例如：

```typst
#raw(read("../../code/main.py"), block: true, lang: "python")
```

PLACEHOLDER：删除本说明并嵌入全部实际程序。若确实未使用程序，改为“本论文没有用到程序”；若确实没有支撑材料，另写“本论文没有支撑材料”。
